package it.polimi.ingsw.controller;

public enum Operation {
    SELECT_NICKNAME,
    SELECT_LOBBY_SIZE,
    SELECT_WORKER,
    MOVE,
    BUILD,
    CHOOSE,
    CHOOSE_GOD,
    PLACE_WORKERS,
    SEND_MESSAGE,
}
